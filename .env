# Target list page (Current tab)
BILL_SITE_INVOICES_URL=https://bills.parliament.nz/bills-proposed-laws?lang=en&Tab=Current

# Puppeteer: set to your Chrome to avoid using the bundled Chromium (macOS path)
PUPPETEER_EXECUTABLE_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"

# Browser mode
HEADLESS=false

# First-run safety cap (remove or raise for full run)
MAX_BILLS=5

# (Optional) We still create these folders; not used for downloads on this site
DOWNLOAD_DIR=./Bill-tracker/pdfs
